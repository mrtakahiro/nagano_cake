<h3>商品一覧</h3>

<table>
  <thead>
    <tr>
      <th>商品ID</th> <th>商品名</th> <th>税抜き価格</th>
    </tr>
  </thead>
  <tbody>
    <% @items.each do |item| %>
    <tr>
      <td><%= @items.item.id %></td>
      <td><%= @items.name %></td>
      <td><%= @items.price %></td>
    </tr>
    <% end %>
  </tbody>
</table>