<h2>商品新規登録</h2>
<%= form_with model: @item do |f| %>

<table>
  <tr>
    <td><span>商品画像</span><%= f.file_field :item_image, placeholder: "商品画像", accept: "image/*" %></td>
    <td><%= f.submit '新規登録' %></td>
  </tr>
</table>
<% end %>